import { InventoryListPipe } from './inventory-list.pipe';

describe('InventoryListPipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryListPipe();
    expect(pipe).toBeTruthy();
  });
});
