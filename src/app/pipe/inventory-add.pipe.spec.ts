import { InventoryAddPipe } from './inventory-add.pipe';

describe('InventoryAddPipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryAddPipe();
    expect(pipe).toBeTruthy();
  });
});
